FROM node:20-alpine

WORKDIR /usr/src/app

# Copy package files
COPY package.json yarn.lock ./

# Copy the rest of the application code
COPY . .

EXPOSE 4000

# Install dependencies and run dev server
# This ensures dependencies are installed even with volume mounts
CMD ["sh", "-c", "yarn install && yarn dev"]

